$(".flash_notice").empty()
$(".flash_notice").append("<%= escape_javascript(flash.discard(:notice))%>")
if(window.location.pathname.indexOf("customers") >= 0){
$("#listing_projects").append("<%= j(render(:partial => @project)) %>");	
}else{
$("#cus_pro_us_listing").prepend("<%= j( render :partial => "projects_index", :locals => { :project => @project })  %>");
$(".button").button();	
}
$(".open_project_update").UIdialogs_edit_links();
$(".activate_project").activate_projects();
$("#new_project")[0].reset();
$(".spinning").hide();