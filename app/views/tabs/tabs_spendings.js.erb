var tab = $("li.active").children("a").attr("href")
$(tab).empty().append("<%= j( render "spendings")  %>")
var firm_format = $(".current_firm_data").data("timeformat")
var hours = secondsToString($(".seconds").data("hours"), firm_format)
$(".seconds").html(hours )
$(".spinning").hide();