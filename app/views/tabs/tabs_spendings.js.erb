$('.currency_to_convert').text(function(){
	var value = parseFloat($(this).attr('data-amount'))
	return currency_converter(value)
	});
$(".seconds").html(secondsToString($(".seconds").data("hours"), $(".current_firm_data").data("timeformat")) )