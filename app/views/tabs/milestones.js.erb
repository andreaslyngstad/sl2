var tab = $("li.active").children("a").attr("href")
$(tab).append("<%= j( render :partial => "milestones/listing_milestones")  %>")
$("#listing_milestones").append("<%= j( render @milestones)  %>")
$("#dialog_milestone").UIdialogs_links();
$('.date_format_setter').set_date_format()
$(".open_milestone_update").UIdialogs_edit_links();
$(".spinning").hide()