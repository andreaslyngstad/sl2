var tab = $("li.active").children("a").attr("href")
$(tab).empty().append("<%= j( render :partial => "projects_tab")  %>")
$("#dialog_project").UIdialogs_links();
$('.date_format_setter').set_date_format()
$(".open_project_update").UIdialogs_edit_links();
$(".spinning").hide()