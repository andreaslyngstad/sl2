$('.flash_error').empty();
$(".flash_notice").empty();
var tab = $("li.active").children("a").attr("href")
$(tab).empty().append("<%= j( render :partial => "invoices/listing_invoices")  %>")
$("#dialog_invoices").UIdialogs_links();
$("#invoices_pr_date_select").chosen({width:'200px'});
$('.date_format_setter').set_date_format()
$(".total_log_time").set_hours()
$('.clock_format_setter').set_clock_format()
$(".range_date").datepicker({ 
		onSelect: function() {
			$('#range_form').submit();
		
  		}
  		}).attr( 'readOnly' , 'true' )

$(".open_invoices_update").UIdialogs_edit_links();
$(".open_log_update").UIdialogs_edit_logs_links();
$(".spinning").hide()