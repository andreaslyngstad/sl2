<div class="log_menu">
	
   <div class="add_some">
       <a id="dialog_log">New log</a>
   </div>
   <div id="dialog_log_form" class="dialog_form" title="Create new log" style="display:none;">
      	<%= form_for(Log.new, :remote => true) do |l| %>  
  			<%= render :partial => "logs/log_menu_form", :locals => {:l => l } %>
    	<% end %>
   </div>
   <div class="range" >	   
	    <%= form_tag('/log_range', :id => "range_form", :remote => true, method: :get) do -%>
	    <%= hidden_field_tag "url", @klass ? @klass.class : "index"  %>
	    <%= hidden_field_tag "id", @klass ? @klass.id : "index"%>
	    <div id="range_date_fields">
		  	<%= text_field_tag 'from',date_format(time_zone_now - 1.week),  :class => "range_date"%>
			<%= text_field_tag 'to', date_format(time_zone_now), :class => "range_date"  %>
		</div>
		<% end %>
   	</div>
 
    <div class="logs_pr_date">
      <select class='logs_pr_date_select' id="logs_pr_date_select" data-url="<%= @klass ? @klass.class : "index" %>" data-id="<%= @klass ? @klass.id : "index" %>">
           <option data-from="" data-to="" value="to_day">To day</option>
           <option value="this_week">This week</option>
           <option value="this_month">This month</option>
           <option value="this_year">This year</option>   
           <option value="yesterday">Yesterday</option>
           <option value="last_week">Last week</option>
           <option value="last_month">Last month</option>
           <option value="last_year">Last year</option>   
          </select>
    </div> 
</div>