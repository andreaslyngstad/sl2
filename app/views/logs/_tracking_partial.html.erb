 <% content_for :help do %>
 <div class="page_help">
	
	<h2><img src="../system/info.png"/> Tracking logs</h2>
	    <p>Your logs are listed here and you can make new logs.</p>
		<p>You can log your time in two ways.</p> 
		<ul>
		<li>1. Write what you are working on and click on the green arrow. When you stop working click on the stop button.
			You can click on the small grey arrow add more details about the log.</li>
		<li>2. You can also add logs by clicking on the "Add log" button.</li>
		
		</ul>
		<p>Tip: If you want to find logs on a particular project or custumer, go to the project or custumer and click on the logs tab.</p>
		</div>
<% end %>
    <div class="tracking_select" style="display: none;">
	<div id="tracking_project_task">
    <%= n.label :project %>
         <select class="searchableS_tracking" id="logProjectIdTracking" log="<%= log.id %>" name="log[project_id]">
           <option value="" data-customer="">None</option>
           <% all_projects.each do |project| %>
                <% if project == log.project  %>
                  <option value="<%= project.id %>" selected="selected"><%= project.name %></option>
                <% else %>
                  <option value="<%= project.id %>"><%= project.name %></option>
                  <% end %>
                 <% end %>
          </select>
          <div><%= n.label :Task %>
          <select class="searchableS_tracking" id="logTodoIdTracking<%= log.id %>" log="<%= log.id %>" name="log[todo_id]">
              <% if log.project.nil? %>
          <option value="" id="value">Select a project</option>
          <%else%>
          <option value>none</option>
          <% log.project.todos.not_complete.each do |todo| %>
          <% if todo == log.todo %>
          <option value="<%= todo.id %>" selected="selected"><%=todo.name%></option>
          <%else%>
          <option value="<%= todo.id %>"><%= todo.name%></option>
          <% end %>
          <% end %>
          <% end %>
          </select>
                      <% if !log.todo.nil? %>
            <% if log.todo.completed == true %>
            <%= n.label :done?%><%= check_box_tag "done", 1 ,true, {:style => "margin:13px 0 0 7px"} %>
            <% else %>
            <%= n.label :done?%><%= check_box_tag "done", 1, false, {:style => "margin:13px 0 0 7px"}  %>
            <% end %>
            <% else %>
            <%= n.label :done?%><%= check_box_tag "done", 1, false, {:style => "margin:13px 0 0 7px"}  %>
            <% end %>
           </div> </div>
           <div id="tracking_customer_employee">
          <%= n.label :customer %>
          	<%= n.select(:customer_id, all_customers.map {|customer| [customer.name, customer.id]},
          	{ :include_blank => "None"}, :class => "searchableS_tracking", :id => "logCustomerIdTracking#{log.id}", :log => "#{log.id}" )%>
       <%= n.label :employee %>
       <select class="searchableS_tracking" id="logEmployeeIdTracking<%= log.id %>" log="<%= log.id %>" name="log[employee_id]">
            
            <% if !log.customer.nil? %>
             <option value>none</option>
            <% log.customer.employees.each do |employee| %>
              <% if employee == log.employee %>
          <option value="<%= employee.id %>" selected="selected"><%=employee.name%></option>
          <%else%>
          <option value="<%= employee.id %>"><%= employee.name%></option>
          <% end %>
            <% end %>
            <%else%>
            <option value="">Select a customer</option>
            <% end %>
        </select>
    
     
      </div>
      </div>
     